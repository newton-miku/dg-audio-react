[project]
name = "dg-audio-react"
version = "0.1.0"
description = "声狼 DG-Audio-React：郊狼3 (DG-Lab 3.0) 电脑声音反应控制"
requires-python = ">=3.10"

[tool.uv]
# 使用 uv 创建 Python 3.12 虚拟环境：uv venv --python 3.12 .venv
